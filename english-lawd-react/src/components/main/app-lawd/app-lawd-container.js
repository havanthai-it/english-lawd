import { connect } from 'react-redux';
import AppLawdView from './app-lawd-view';

const mapStateToProps = (state) => {
  return {
  }
}

const mapDispatchToProps = (dispatch) => {
  return {
  }
}

const AppLawd = connect(mapStateToProps, mapDispatchToProps)(AppLawdView);

export default AppLawd;
