import { connect } from 'react-redux';
import SignupSuccessBoxView from './signup-success-box-view';

const mapStateToProps = (state) => {
  return {
  };
}

const mapDispatchToProps = (dispatch) => {
  return {
  };
};

const SignupSuccessBox = connect(mapStateToProps, mapDispatchToProps)(SignupSuccessBoxView)

export default SignupSuccessBox;
